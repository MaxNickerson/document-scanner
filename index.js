import React from 'react';
import TakePhoto from '../components/TakePhoto';

const Home = () => (
    <div>
        <h1>Take a Photo</h1>
        <TakePhoto />
    </div>
);

export default Home;
